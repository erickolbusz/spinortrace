# spinortrace

This is a sentence with $\gamma^\mu p_1^+$ inline math

test\
$$ \textrm{tr}\,\left[\gamma^{\perp a}\right]\,G_{\alpha\beta}(l) $$
test

When $a \ne 0$, there are two solutions to $(ax^2 + bx + c = 0)$ and they are
$$ x = {-b \pm \sqrt{b^2-4ac} \over 2a} $$